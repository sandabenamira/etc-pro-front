import React from 'react';
import { shallow } from 'enzyme';
import RegistreAppel from './index';

// const setUp = (props={}) => {
//     const component = shallow(<RegistreAppel {...props} />);
//     return component;
// };
describe(' Registre Component', () => {

    // let component;
    // beforeEach(() => {
    //     component = setUp(); 
    // });

    it('test 1: it should render without errors ', () => {

        expect(1).toBe(1);
    });

    // it('Should render a component', () => {
    //     const divRegistre = component.find(`[data-test='app-wrapper']`);
    //     expect(divRegistre.length).toBe(1);  
    // });



});
